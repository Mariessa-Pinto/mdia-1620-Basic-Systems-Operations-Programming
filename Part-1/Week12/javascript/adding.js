/**
 * @desc adds the two values
 * @param {*} a the value of a
 * @param {*} b the value of b
 * @returns the added value of a and b
 */
function sum(a, b){
    return a + b;
}

module.exports = sum;